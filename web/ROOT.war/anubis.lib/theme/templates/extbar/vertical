<%
   def xclass = OPTIONS.class? OPTIONS.class+'-' : '';
%>
<table class="rui-sidemenu ${xclass}extbar" cellpadding="0" cellspacing="0" border="0">
	<% for ( o in DATA ) {%>
		<tr>
			<td><a href="#${o.name}">${o.title}</a></td>
		</tr>
	<% } %>
</table>
